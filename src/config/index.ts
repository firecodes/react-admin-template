/**
 * @description 配置应用程序的常量,一般用于统一管理应用程序的变量
 */

// 首页的路径
export const HOME_PATH: string = '/home/index'

// 登录页的路径
export const LOGIN_PATH: string = '/login'

// 路由白名单（也指静态路由）
export const WHITE_LIST: string[] = ['/403', '/404', '/500']
